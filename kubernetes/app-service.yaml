apiVersion: v1
kind: Service

metadata:
  name: devops-jr-app-service
  namespace: devops

spec:

  type: NodePort

  selector:
    app: devops-jr-app

  ports:

  - port: 5000
    targetPort: 5000
    nodePort: 30050
